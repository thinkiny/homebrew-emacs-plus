diff --git a/src/frame.c b/src/frame.c
index ada3e4ad730..3dc8e29fce9 100644
--- a/src/frame.c
+++ b/src/frame.c
@@ -1210,6 +1210,7 @@ make_frame (bool mini_p)
 #ifdef NS_IMPL_COCOA
   f->ns_appearance = ns_appearance_system_default;
   f->ns_transparent_titlebar = false;
+  f->ns_background_blur = 0;
 #endif
 #endif
   f->select_mini_window_flag = false;
@@ -4885,6 +4886,7 @@ DEFUN ("frame-scale-factor", Fframe_scale_factor, Sframe_scale_factor,
 #ifdef NS_IMPL_COCOA
   {"ns-appearance",		SYMBOL_INDEX (Qns_appearance)},
   {"ns-transparent-titlebar",	SYMBOL_INDEX (Qns_transparent_titlebar)},
+  {"ns-background-blur",	SYMBOL_INDEX (Qns_background_blur)},
 #endif
 };
 
@@ -7321,6 +7323,7 @@ syms_of_frame (void)
 #ifdef NS_IMPL_COCOA
   DEFSYM (Qns_appearance, "ns-appearance");
   DEFSYM (Qns_transparent_titlebar, "ns-transparent-titlebar");
+  DEFSYM (Qns_background_blur, "ns-background-blur");
 #endif
 
   DEFSYM (Qalpha, "alpha");
diff --git a/src/frame.h b/src/frame.h
index 7873c90c2f2..98e8d03b6bb 100644
--- a/src/frame.h
+++ b/src/frame.h
@@ -740,6 +740,7 @@ #define EMACS_FRAME_H
   /* NSAppearance theme used on this frame.  */
   enum ns_appearance_type ns_appearance;
   bool_bf ns_transparent_titlebar;
+  int ns_background_blur;
 #endif
 
 #ifdef HAVE_TEXT_CONVERSION
@@ -1262,6 +1263,7 @@ #define FRAME_TOOLTIP_P(f) ((f)->tooltip)
 #ifdef NS_IMPL_COCOA
 #define FRAME_NS_APPEARANCE(f) ((f)->ns_appearance)
 #define FRAME_NS_TRANSPARENT_TITLEBAR(f) ((f)->ns_transparent_titlebar)
+#define FRAME_NS_BACKGROUND_BLUR(f) ((f)->ns_background_blur)
 #endif
 #else /* not HAVE_WINDOW_SYSTEM */
 #define FRAME_OVERRIDE_REDIRECT(f) ((void) (f), 0)
diff --git a/src/macfont.m b/src/macfont.m
index a24ff046735..a469d9ff0ca 100644
--- a/src/macfont.m
+++ b/src/macfont.m
@@ -3030,9 +3030,15 @@ So we use CTFontDescriptorCreateMatchingFontDescriptor (no
             CG_SET_FILL_COLOR_WITH_FACE_FOREGROUND (context, face);
           else
             CG_SET_FILL_COLOR_WITH_FRAME_CURSOR (context, f);
+
+          CGContextSetAlpha (context, 1);
         }
       else
-        CG_SET_FILL_COLOR_WITH_FACE_BACKGROUND (context, face);
+        {
+          CGContextSetAlpha (context, f->alpha_background);
+          CG_SET_FILL_COLOR_WITH_FACE_BACKGROUND (context, face);
+        }
+      CGContextClearRect (context, background_rect);
       CGContextFillRects (context, &background_rect, 1);
     }
 
@@ -3041,6 +3047,7 @@ So we use CTFontDescriptorCreateMatchingFontDescriptor (no
       CGAffineTransform atfm;
 
       CGContextScaleCTM (context, 1, -1);
+      CGContextSetAlpha (context, 1);
       if (s->hl == DRAW_CURSOR)
         {
           if (face && (NS_FACE_BACKGROUND (face)
diff --git a/src/nsfns.m b/src/nsfns.m
index 91483f91b76..8063cf1c710 100644
--- a/src/nsfns.m
+++ b/src/nsfns.m
@@ -295,7 +295,7 @@ Turn the input menu (an NSMenu) into a lisp list for tracking on lisp side.
   struct face *face;
   NSColor *col;
   NSView *view = FRAME_NS_VIEW (f);
-  EmacsCGFloat alpha;
+  EmacsCGFloat alpha = f->alpha_background;
 
   block_input ();
   if (ns_lisp_to_color (arg, &col))
@@ -310,23 +310,28 @@ Turn the input menu (an NSMenu) into a lisp list for tracking on lisp side.
   f->output_data.ns->background_color = col;
 
   FRAME_BACKGROUND_PIXEL (f) = [col unsignedLong];
-  alpha = [col alphaComponent];
-
   if (view != nil)
     {
-      [[view window] setBackgroundColor: col];
+      [[view window] setBackgroundColor: [col colorWithAlphaComponent: alpha]];
 
-      if (alpha != (EmacsCGFloat) 1.0)
+      EmacsWindow *ew = (EmacsWindow *) [view window];
+      if (alpha != (EmacsCGFloat) 1.0 || (ew && [ew hasGlassView]))
           [[view window] setOpaque: NO];
       else
           [[view window] setOpaque: YES];
+#ifdef NS_IMPL_COCOA
+      ns_update_background_blur (f);
+      {
+        EmacsWindow *window = (EmacsWindow *) [view window];
+        if (window)
+          [window updateWindowGlassWithColor: col alpha: alpha];
+      }
+#endif
 
       face = FRAME_DEFAULT_FACE (f);
       if (face)
         {
-          col = [NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND (face)];
-          face->background = [[col colorWithAlphaComponent: alpha]
-                               unsignedLong];
+          face->background = [col unsignedLong];
 
           update_face_from_frame_parameter (f, Qbackground_color, arg);
         }
@@ -340,6 +345,55 @@ Turn the input menu (an NSMenu) into a lisp list for tracking on lisp side.
   unblock_input ();
 }
 
+static void
+ns_set_alpha_background (struct frame *f, Lisp_Object arg, Lisp_Object oldval)
+{
+  NSView *view = FRAME_NS_VIEW (f);
+  double alpha = 1.0;
+
+  if (NILP (arg))
+    alpha = 1.0;
+  else if (FLOATP (arg))
+    {
+      alpha = XFLOAT_DATA (arg);
+      if (! (0 <= alpha && alpha <= 1.0))
+	args_out_of_range (make_float (0.0), make_float (1.0));
+    }
+  else if (FIXNUMP (arg))
+    {
+      EMACS_INT ialpha = XFIXNUM (arg);
+      if (! (0 <= ialpha && ialpha <= 100))
+	args_out_of_range (make_fixnum (0), make_fixnum (100));
+      alpha = ialpha / 100.0;
+    }
+  else
+    wrong_type_argument (Qnumberp, arg);
+
+  f->alpha_background = alpha;
+  if (view != nil)
+    {
+      [[view window] setBackgroundColor:
+		     [f->output_data.ns->background_color
+		       colorWithAlphaComponent: alpha]];
+      EmacsWindow *ew = (EmacsWindow *) [view window];
+      if (alpha != 1.0 || (ew && [ew hasGlassView]))
+	[[view window] setOpaque: NO];
+      else
+	[[view window] setOpaque: YES];
+    }
+  recompute_basic_faces (f);
+  SET_FRAME_GARBAGED (f);
+#ifdef NS_IMPL_COCOA
+  ns_update_background_blur (f);
+  {
+    EmacsWindow *window = (EmacsWindow *) [view window];
+    if (window)
+      [window updateWindowGlassWithColor: f->output_data.ns->background_color
+                                   alpha: alpha];
+  }
+#endif
+}
+
 
 static void
 ns_set_cursor_color (struct frame *f, Lisp_Object arg, Lisp_Object oldval)
@@ -1106,12 +1160,14 @@ Turn the input menu (an NSMenu) into a lisp list for tracking on lisp side.
   ns_set_z_group,
   0, /* x_set_override_redirect */
   gui_set_no_special_glyphs,
-  gui_set_alpha_background,
+  ns_set_alpha_background,
   gui_set_borders_respect_alpha_background,
   NULL,
 #ifdef NS_IMPL_COCOA
   ns_set_appearance,
   ns_set_transparent_titlebar,
+  ns_set_background_blur,
+  NULL,
 #endif
 };
 
@@ -1204,6 +1260,7 @@ Turn the input menu (an NSMenu) into a lisp list for tracking on lisp side.
   Lisp_Object display;
   struct ns_display_info *dpyinfo = NULL;
   Lisp_Object parent, parent_frame;
+  Lisp_Object default_alpha_background = Qnil;
   struct kboard *kb;
   static int desc_ctr = 1;
   NSWindow *main_window = [NSApp mainWindow];
@@ -1422,6 +1479,9 @@ Turn the input menu (an NSMenu) into a lisp list for tracking on lisp side.
   FRAME_NS_TRANSPARENT_TITLEBAR (f) = !NILP (tem) && !EQ (tem, Qunbound);
   store_frame_param (f, Qns_transparent_titlebar,
                      FRAME_NS_TRANSPARENT_TITLEBAR (f) ? Qt : Qnil);
+
+  gui_default_parameter (f, parms, Qns_background_blur, Qnil,
+                         NULL, NULL, RES_TYPE_NUMBER);
 #endif
 
   parent_frame = gui_display_get_arg (dpyinfo, parms, Qparent_frame, NULL, NULL,
@@ -1436,6 +1496,9 @@ Turn the input menu (an NSMenu) into a lisp list for tracking on lisp side.
 
   fset_parent_frame (f, parent_frame);
   store_frame_param (f, Qparent_frame, parent_frame);
+  if (FRAME_PARENT_FRAME (f))
+    default_alpha_background =
+      make_float (FRAME_PARENT_FRAME (f)->alpha_background);
 
   gui_default_parameter (f, parms, Qz_group, Qnil, NULL, NULL, RES_TYPE_SYMBOL);
   gui_default_parameter (f, parms, Qno_focus_on_map, Qnil,
@@ -1539,7 +1602,7 @@ Turn the input menu (an NSMenu) into a lisp list for tracking on lisp side.
                          RES_TYPE_NUMBER);
   gui_default_parameter (f, parms, Qalpha, Qnil,
                          "alpha", "Alpha", RES_TYPE_NUMBER);
-  gui_default_parameter (f, parms, Qalpha_background, Qnil,
+  gui_default_parameter (f, parms, Qalpha_background, default_alpha_background,
                          "alphaBackground", "AlphaBackground", RES_TYPE_NUMBER);
   gui_default_parameter (f, parms, Qborders_respect_alpha_background, Qnil,
                          "bordersRespectAlphaBackground",
diff --git a/src/nsterm.h b/src/nsterm.h
index 5b9762826de..eb6bd024422 100644
--- a/src/nsterm.h
+++ b/src/nsterm.h
@@ -427,6 +427,8 @@ #define NSTRACE_UNSILENCE()
   enum ns_return_frame_mode dnd_mode;
   BOOL dnd_allow_same_frame;
   BOOL dnd_move_tooltip_with_frame;
+  NSView *title_tint_view;
+  NSView *window_glass_view;
 }
 
 #ifdef NS_IMPL_GNUSTEP
@@ -442,6 +444,11 @@ #define NSTRACE_UNSILENCE()
 - (NSInteger) borderWidth;
 - (BOOL) restackWindow: (NSWindow *) win above: (BOOL) above;
 - (void) setAppearance;
+- (void) updateTitleTintWithColor: (NSColor *)baseColor
+                            alpha: (CGFloat)alpha;
+- (void) updateWindowGlassWithColor: (NSColor *)baseColor
+                              alpha: (CGFloat)alpha;
+- (BOOL) hasGlassView;
 - (void) setLastDragEvent: (NSEvent *) event;
 - (NSDragOperation) beginDrag: (NSDragOperation) op
 		forPasteboard: (NSPasteboard *) pasteboard
@@ -1244,6 +1251,9 @@ #define NSAPP_DATA2_RUNFILEDIALOG 11
 extern void ns_set_transparent_titlebar (struct frame *f,
                                          Lisp_Object new_value,
                                          Lisp_Object old_value);
+extern void ns_set_background_blur (struct frame *f, Lisp_Object new_value,
+                                    Lisp_Object old_value);
+extern void ns_update_background_blur (struct frame *f);
 #endif
 extern int ns_select (int nfds, fd_set *readfds, fd_set *writefds,
 		      fd_set *exceptfds, struct timespec *timeout,
diff --git a/src/nsterm.m b/src/nsterm.m
index 559b7160044..b0cdd2fd8f6 100644
--- a/src/nsterm.m
+++ b/src/nsterm.m
@@ -31,6 +31,7 @@ Updated by Christian Limpach (chris@nice.ch)
 #include <config.h>
 
 #include <fcntl.h>
+#include <limits.h>
 #include <math.h>
 #include <pthread.h>
 #include <sys/types.h>
@@ -71,6 +72,10 @@ Updated by Christian Limpach (chris@nice.ch)
 #include "macfont.h"
 #include <Carbon/Carbon.h>
 #include <IOSurface/IOSurface.h>
+extern int CGSSetWindowBackgroundBlurRadius (int connection,
+                                             int windowNumber,
+                                             int radius);
+extern int CGSDefaultConnectionForThread (void);
 /* ApplicationServices provides the macOS accessibility Zoom API
    UAZoomEnabled and UAZoomChangeFocus (UniversalAccess framework).
    Carbon.h already pulls in ApplicationServices on most SDK versions,
@@ -1670,6 +1675,9 @@ -(void)remove
           ns_set_offset(f, f->left_pos, f->top_pos, 0);
         }
     }
+#ifdef NS_IMPL_COCOA
+    ns_update_background_blur (f);
+#endif
 }
 
 
@@ -2225,6 +2233,77 @@ Hide the window (X11 semantics)
     }
 #endif /* MAC_OS_X_VERSION_MAX_ALLOWED >= 101000 */
 }
+
+static void
+ns_apply_background_blur (struct frame *f)
+{
+  EmacsView *view = (EmacsView *)FRAME_NS_VIEW (f);
+  NSWindow *window;
+  NSColor *color;
+  CGFloat alpha = 1.0;
+  int radius = FRAME_NS_BACKGROUND_BLUR (f);
+
+  if (view == nil)
+    return;
+
+  window = [view window];
+  if (window == nil)
+    return;
+
+  color = f->output_data.ns->background_color;
+  if (color)
+    alpha = [color alphaComponent];
+  if (f->alpha_background < alpha)
+    alpha = f->alpha_background;
+
+  if (radius < 0)
+    radius = 0;
+
+  if (alpha >= 1.0 || radius == 0)
+    radius = 0;
+
+  CGSSetWindowBackgroundBlurRadius (CGSDefaultConnectionForThread (),
+                                    (int)[window windowNumber],
+                                    radius);
+}
+
+void
+ns_update_background_blur (struct frame *f)
+{
+  ns_apply_background_blur (f);
+}
+
+void
+ns_set_background_blur (struct frame *f, Lisp_Object new_value,
+                        Lisp_Object old_value)
+{
+  int blur = 0;
+
+  NSTRACE ("ns_set_background_blur");
+
+  if (NILP (new_value))
+    blur = 0;
+  else if (FIXNUMP (new_value))
+    {
+      EMACS_INT val = XFIXNUM (new_value);
+      if (val < 0)
+        error ("Invalid `ns-background-blur' value");
+      blur = (val > INT_MAX) ? INT_MAX : (int) val;
+    }
+  else if (FLOATP (new_value))
+    {
+      double val = XFLOAT_DATA (new_value);
+      if (val < 0)
+        error ("Invalid `ns-background-blur' value");
+      blur = (val > INT_MAX) ? INT_MAX : (int) val;
+    }
+  else
+    error ("Invalid `ns-background-blur' value");
+
+  FRAME_NS_BACKGROUND_BLUR (f) = blur;
+  if (FRAME_NS_VIEW (f) != nil)
+    ns_apply_background_blur (f);
+}
 #endif /* NS_IMPL_COCOA */
 
 static void
@@ -2870,8 +2949,13 @@ Hide the window (X11 semantics)
 
   block_input ();
   ns_focus (f, &r, 1);
-  [[NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND
-			    (FACE_FROM_ID (f, DEFAULT_FACE_ID))] set];
+  {
+    NSColor *color =
+      [NSColor colorWithUnsignedLong:
+                 NS_FACE_BACKGROUND (FACE_FROM_ID (f, DEFAULT_FACE_ID))];
+    color = [color colorWithAlphaComponent: f->alpha_background];
+    [color set];
+  }
   NSRectFill (r);
   ns_unfocus (f);
 
@@ -2899,7 +2983,12 @@ Hide the window (X11 semantics)
 
   r = NSIntersectionRect (r, [view frame]);
   ns_focus (f, &r, 1);
-  [[NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND (face)] set];
+  {
+    NSColor *color =
+      [NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND (face)];
+    color = [color colorWithAlphaComponent: f->alpha_background];
+    [color set];
+  }
 
   NSRectFill (r);
 
@@ -3003,7 +3092,12 @@ Hide the window (X11 semantics)
         return;
 
       ns_focus (f, NULL, 1);
-      [[NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND (face)] set];
+      {
+	NSColor *color =
+	  [NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND (face)];
+	  color = [color colorWithAlphaComponent: f->alpha_background];
+	[color set];
+      }
       NSRectFill (NSMakeRect (0, margin, width, border));
       NSRectFill (NSMakeRect (0, 0, border, height));
       NSRectFill (NSMakeRect (0, margin, width, border));
@@ -3055,7 +3149,12 @@ Hide the window (X11 semantics)
           NSRect r = NSMakeRect (0, y, FRAME_PIXEL_WIDTH (f), height);
           ns_focus (f, &r, 1);
 
-          [[NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND (face)] set];
+          {
+	    NSColor *color =
+	      [NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND (face)];
+	    color = [color colorWithAlphaComponent: f->alpha_background];
+	    [color set];
+          }
           NSRectFill (NSMakeRect (0, y, width, height));
           NSRectFill (NSMakeRect (FRAME_PIXEL_WIDTH (f) - width,
                                   y, width, height));
@@ -3306,7 +3405,11 @@ Hide the window (X11 semantics)
     {
       NSTRACE_RECT ("clearRect", bmpRect);
 
-      [[NSColor colorWithUnsignedLong:face->background] set];
+      {
+	NSColor *color = [NSColor colorWithUnsignedLong:face->background];
+	color = [color colorWithAlphaComponent: f->alpha_background];
+	[color set];
+      }
       NSRectFill (bmpRect);
     }
 
@@ -3337,6 +3440,7 @@ Hide the window (X11 semantics)
       else
         bm_color = f->output_data.ns->cursor_color;
 
+      bm_color = [bm_color colorWithAlphaComponent:f->alpha_background];
       [bm_color set];
       [bmp fill];
 
@@ -3912,6 +4016,9 @@ larger if there are taller display elements (e.g., characters
   if (color == nil)
     color = [NSColor grayColor];
 
+  if (s->hl != DRAW_CURSOR)
+    color = [color colorWithAlphaComponent: s->f->alpha_background];
+
   if (color != di->relief_background_color)
     {
       [di->relief_background_color release];
@@ -4131,8 +4238,10 @@ Function modeled after x_draw_glyph_string_box ().
   /* TODO: Sometimes box_color is 0 and this seems wrong; should investigate.  */
   if (s->face->box == FACE_SIMPLE_BOX && s->face->box_color)
     {
+      NSColor *color = [NSColor colorWithUnsignedLong:face->box_color];
+      color = [color colorWithAlphaComponent: s->f->alpha_background];
       ns_draw_box (r, abs (hthickness), abs (vthickness),
-                   [NSColor colorWithUnsignedLong:face->box_color],
+                   color,
                    left_p, right_p);
     }
   else
@@ -4165,7 +4274,12 @@ Function modeled after x_draw_glyph_string_box ().
 #ifdef NS_IMPL_COCOA
 	  /* On cocoa emacs the stipple is stored as a mask CGImage.
 	     First we want to clear the background with the bg color.  */
-	  [[NSColor colorWithUnsignedLong:face->background] set];
+	  {
+	    NSColor *color =
+	      [NSColor colorWithUnsignedLong:face->background];
+	    color = [color colorWithAlphaComponent: s->f->alpha_background];
+	    [color set];
+	  }
 	  r = NSMakeRect (s->x, s->y + box_line_width,
 			  s->background_width,
 			  s->height - 2 * box_line_width);
@@ -4209,9 +4323,14 @@ Function modeled after x_draw_glyph_string_box ().
 	       || force_p)
 	{
 	  if (s->hl != DRAW_CURSOR)
-	    [(NS_FACE_BACKGROUND (face) != 0
-	      ? [NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND (face)]
-	      : FRAME_BACKGROUND_COLOR (s->f)) set];
+	    {
+	      NSColor *color =
+		(NS_FACE_BACKGROUND (face) != 0
+		 ? [NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND (face)]
+		 : FRAME_BACKGROUND_COLOR (s->f));
+	      color = [color colorWithAlphaComponent: s->f->alpha_background];
+	      [color set];
+	    }
 	  else if (face && (NS_FACE_BACKGROUND (face)
 			    == [(NSColor *) FRAME_CURSOR_COLOR (s->f)
 					    unsignedLong]))
@@ -4351,7 +4470,12 @@ Function modeled after x_draw_glyph_string_box ().
      otherwise, since we composite the image under NS (instead of mucking
      with its background color), we must clear just the image area.  */
 
-  [[NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND (face)] set];
+  {
+    NSColor *color =
+      [NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND (face)];
+    color = [color colorWithAlphaComponent: s->f->alpha_background];
+    [color set];
+  }
 
   if (bg_height > s->slice.height || s->img->hmargin || s->img->vmargin
       || s->img->mask || s->img->pixmap == 0 || s->width != s->background_width)
@@ -4422,6 +4546,7 @@ Function modeled after x_draw_glyph_string_box ().
     {
       [FRAME_CURSOR_COLOR (s->f) set];
       tdCol = [NSColor colorWithUnsignedLong: NS_FACE_BACKGROUND (face)];
+      tdCol = [tdCol colorWithAlphaComponent: s->f->alpha_background];
     }
   else
     tdCol = [NSColor colorWithUnsignedLong: NS_FACE_FOREGROUND (face)];
@@ -4514,10 +4639,22 @@ Function modeled after x_draw_glyph_string_box ().
 		face = FACE_FROM_ID (s->f, MOUSE_FACE_ID);
 	      prepare_face_for_display (s->f, face);
 
-	      [[NSColor colorWithUnsignedLong: face->background] set];
+	      {
+		NSColor *color =
+		  [NSColor colorWithUnsignedLong: face->background];
+		  color = [color colorWithAlphaComponent:
+				 s->f->alpha_background];
+		[color set];
+	      }
 	    }
 	  else
-	    [[NSColor colorWithUnsignedLong: s->face->background] set];
+	    {
+	      NSColor *color =
+		[NSColor colorWithUnsignedLong: s->face->background];
+		color = [color colorWithAlphaComponent:
+			       s->f->alpha_background];
+	      [color set];
+	    }
 	  NSRectFill (NSMakeRect (x, y, w, h));
 	}
     }
@@ -4551,7 +4688,13 @@ Function modeled after x_draw_glyph_string_box ().
 	      /* On cocoa emacs the stipple is stored as a mask CGImage.
 		 First we want to clear the background with the bg
 		 color.  */
-	      [[NSColor colorWithUnsignedLong:s->face->background] set];
+	      {
+		NSColor *color =
+		  [NSColor colorWithUnsignedLong:s->face->background];
+		  color = [color colorWithAlphaComponent:
+				 s->f->alpha_background];
+		[color set];
+	      }
 	      NSRectFill (NSMakeRect (x, s->y, background_width, s->height));
 
 	      /* This part could possibly be improved, the author is
@@ -4579,7 +4722,13 @@ Function modeled after x_draw_glyph_string_box ().
 #endif /* NS_IMPL_COCOA */
 	    }
 	  else
-	    [[NSColor colorWithUnsignedLong: s->face->background] set];
+	    {
+	      NSColor *color =
+		[NSColor colorWithUnsignedLong: s->face->background];
+		color = [color colorWithAlphaComponent:
+			       s->f->alpha_background];
+	      [color set];
+	    }
 
 	  NSRectFill (NSMakeRect (x, s->y, background_width, s->height));
 	}
@@ -9220,8 +9369,12 @@ - (void)toggleFullScreen: (id)sender
         }
 
       [w setContentView:[fw contentView]];
-      [w setBackgroundColor: col];
-      if ([col alphaComponent] != (EmacsCGFloat) 1.0)
+      if ([(EmacsWindow *)w hasGlassView])
+        [w setBackgroundColor: [NSColor clearColor]];
+      else
+        [w setBackgroundColor: [col colorWithAlphaComponent:
+                                      f->alpha_background]];
+      if ([(EmacsWindow *)w hasGlassView] || f->alpha_background != (EmacsCGFloat) 1.0)
         [w setOpaque: NO];
 
       f->border_width = [w borderWidth];
@@ -9873,12 +10026,15 @@ - (instancetype) initWithEmacsFrame: (struct frame *) f
 {
   NSWindowStyleMask styleMask;
   int width, height;
+  BOOL undecorated = FRAME_UNDECORATED (f);
+  BOOL undecorated_round = FRAME_UNDECORATED_ROUND (f);
+  BOOL use_full_size_content = !f->tooltip && (!undecorated || undecorated_round);
 
   NSTRACE ("[EmacsWindow initWithEmacsFrame:fullscreen:screen:]");
 
   if (fullscreen)
     styleMask = NSWindowStyleMaskBorderless;
-  else if (FRAME_UNDECORATED (f))
+  else if (undecorated)
     {
       styleMask = NSWindowStyleMaskBorderless;
 #ifdef NS_IMPL_COCOA
@@ -9893,10 +10049,8 @@ - (instancetype) initWithEmacsFrame: (struct frame *) f
 		 | NSWindowStyleMaskMiniaturizable
 		 | NSWindowStyleMaskClosable);
 
-  if (FRAME_UNDECORATED_ROUND (f))
-    {
-      styleMask |= NSFullSizeContentViewWindowMask;
-    }
+  if (use_full_size_content)
+    styleMask |= NSFullSizeContentViewWindowMask;
 
   last_drag_event = nil;
 
@@ -9916,7 +10070,61 @@ - (instancetype) initWithEmacsFrame: (struct frame *) f
       EmacsView *view = FRAME_NS_VIEW (f);
 
       [self setDelegate:view];
-      [[self contentView] addSubview:view];
+
+      NSView *boundingView = [[NSView alloc] initWithFrame:NSMakeRect (0, 0, 0, 0)];
+
+      if (FRAME_PARENT_FRAME (f))
+        {
+          NSGlassEffectView *effectView =
+            [[NSGlassEffectView alloc] initWithFrame:NSMakeRect (0, 0, width, height)];
+          [effectView setAutoresizingMask:NSViewWidthSizable | NSViewHeightSizable];
+          effectView.style = NSGlassEffectViewStyleRegular;
+          [[self contentView] addSubview:effectView];
+          window_glass_view = effectView;
+
+          [effectView addSubview:boundingView];
+
+          NSLayoutGuide *layout = self.contentLayoutGuide;
+
+          boundingView.translatesAutoresizingMaskIntoConstraints = NO;
+          [boundingView.bottomAnchor constraintEqualToAnchor:layout.bottomAnchor].active = YES;
+          [boundingView.leftAnchor constraintEqualToAnchor:layout.leftAnchor].active = YES;
+          [boundingView.rightAnchor constraintEqualToAnchor:layout.rightAnchor].active = YES;
+          [boundingView.topAnchor constraintEqualToAnchor:self.contentView.topAnchor].active = YES;
+        }
+      else
+        {
+          [[self contentView] addSubview:boundingView];
+
+          NSLayoutGuide *layout = self.contentLayoutGuide;
+
+          boundingView.translatesAutoresizingMaskIntoConstraints = NO;
+          [boundingView.bottomAnchor constraintEqualToAnchor:layout.bottomAnchor].active = YES;
+          [boundingView.leftAnchor constraintEqualToAnchor:layout.leftAnchor].active = YES;
+          [boundingView.rightAnchor constraintEqualToAnchor:layout.rightAnchor].active = YES;
+
+          if (undecorated_round || undecorated)
+            [boundingView.topAnchor constraintEqualToAnchor:self.contentView.topAnchor].active = YES;
+          else
+            [boundingView.topAnchor constraintEqualToAnchor:layout.topAnchor].active = YES;
+
+          if (!undecorated && !undecorated_round && !f->tooltip)
+            {
+              title_tint_view = [[NSView alloc] initWithFrame:NSZeroRect];
+              title_tint_view.wantsLayer = YES;
+              title_tint_view.translatesAutoresizingMaskIntoConstraints = NO;
+              [[self contentView] addSubview:title_tint_view];
+
+              [title_tint_view.topAnchor constraintEqualToAnchor:self.contentView.topAnchor].active = YES;
+              [title_tint_view.leftAnchor constraintEqualToAnchor:self.contentView.leftAnchor].active = YES;
+              [title_tint_view.rightAnchor constraintEqualToAnchor:self.contentView.rightAnchor].active = YES;
+              [title_tint_view.bottomAnchor constraintEqualToAnchor:layout.topAnchor].active = YES;
+            }
+        }
+
+      [boundingView addSubview:view];
+      [boundingView release];
+
       [self makeFirstResponder:view];
 
 #if !defined (NS_IMPL_COCOA) || MAC_OS_X_VERSION_MIN_REQUIRED <= 1090
@@ -9939,7 +10147,7 @@ - (instancetype) initWithEmacsFrame: (struct frame *) f
 
 #if defined (NS_IMPL_COCOA) && MAC_OS_X_VERSION_MAX_ALLOWED >= 101000
       if ([self respondsToSelector:@selector(titlebarAppearsTransparent)])
-        [self setTitlebarAppearsTransparent:FRAME_NS_TRANSPARENT_TITLEBAR (f)];
+        [self setTitlebarAppearsTransparent:YES];
 #endif
 
       [self setParentChildRelationships];
@@ -9964,10 +10172,18 @@ - (instancetype) initWithEmacsFrame: (struct frame *) f
       f->border_width = [self borderWidth];
 
       col = [NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND
-                                     (FACE_FROM_ID (f, DEFAULT_FACE_ID))];
-      [self setBackgroundColor:col];
-      if ([col alphaComponent] != (EmacsCGFloat) 1.0)
+                     (FACE_FROM_ID (f, DEFAULT_FACE_ID))];
+      if (window_glass_view != nil)
+        [self setBackgroundColor: [NSColor clearColor]];
+      else
+        [self setBackgroundColor:
+                [col colorWithAlphaComponent:f->alpha_background]];
+      if (window_glass_view != nil || f->alpha_background != (EmacsCGFloat) 1.0)
         [self setOpaque:NO];
+#ifdef NS_IMPL_COCOA
+      ns_update_background_blur (f);
+      [self updateWindowGlassWithColor: col alpha: f->alpha_background];
+#endif
 
       /* toolbar support */
       [self createToolbar:f];
@@ -10002,6 +10218,44 @@ may be chosen (which is undesirable).  */
 }
 
 
+- (void) updateTitleTintWithColor: (NSColor *)baseColor
+                            alpha: (CGFloat)alpha
+{
+  if (title_tint_view == nil || baseColor == nil)
+    return;
+
+#ifdef NS_IMPL_COCOA
+  CGFloat clampedAlpha = MIN (1.0, MAX (0.0, alpha));
+  title_tint_view.layer.backgroundColor =
+    [[baseColor colorWithAlphaComponent:clampedAlpha] CGColor];
+#endif
+}
+
+
+- (void) updateWindowGlassWithColor: (NSColor *)baseColor
+                              alpha: (CGFloat)alpha
+{
+  if (baseColor == nil)
+    return;
+
+  [self updateTitleTintWithColor: baseColor alpha: alpha];
+
+  if (window_glass_view != nil)
+    {
+      [self setBackgroundColor: [NSColor clearColor]];
+      if ([window_glass_view respondsToSelector:@selector(setTintColor:)])
+        [(id)window_glass_view setTintColor:
+              [baseColor colorWithAlphaComponent:alpha * 0.5]];
+    }
+}
+
+
+- (BOOL) hasGlassView
+{
+  return window_glass_view != nil;
+}
+
+
 - (void)createToolbar: (struct frame *)f
 {
   if (FRAME_UNDECORATED (f) || FRAME_UNDECORATED_ROUND (f)
@@ -10040,6 +10294,12 @@ - (void)dealloc
 {
   NSTRACE ("[EmacsWindow dealloc]");
 
+  [title_tint_view release];
+  title_tint_view = nil;
+
+  [window_glass_view release];
+  window_glass_view = nil;
+
   /* We need to release the toolbar ourselves.  */
   [[self toolbar] release];
   [self setToolbar: nil];
@@ -10092,14 +10352,7 @@ - (void)setParentChildRelationships
   [ourView updateCollectionBehavior];
 #endif
 
-  /* Child frames are often used in ways that may mean they should
-     "disappear" into the contents of the parent frame.  macOs's
-     drop-shadows break this effect, so remove them on undecorated
-     child frames.  */
-  if (parentFrame && FRAME_UNDECORATED (ourFrame))
-    [self setHasShadow:NO];
-  else
-    [self setHasShadow:YES];
+  [self setHasShadow:YES];
 #endif
 
 
@@ -10554,6 +10807,10 @@ - (void)setAppearance
          [NSAppearance appearanceNamed:NSAppearanceNameAqua];
 
    [self setAppearance:appearance];
+   [self updateTitleTintWithColor:
+           [NSColor colorWithUnsignedLong:NS_FACE_BACKGROUND
+                      (FACE_FROM_ID (f, DEFAULT_FACE_ID))]
+                                alpha: f->alpha_background];
 #endif /* MAC_OS_X_VERSION_MAX_ALLOWED >= 101000 */
 }
 
